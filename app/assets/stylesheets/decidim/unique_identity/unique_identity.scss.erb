.verification_attachment__upload {
  input {
    display: none
  }

  label {
    cursor: pointer;
    border: 1px solid var(--primary);
    padding: 1em 1em;
    margin: 1rem 0 1rem 0;
    text-align: center;
    display: block;
    right: .2em;
    vertical-align: middle;
    border-radius: 4px;
    transition: background-color 0.25s ease-out, color 0.25s ease-out;
    font-size: 1rem;
    color: var(--primary);
    line-height: 1;

    &:hover {
      border: 1px solid transparent;
      background-color: var(--primary);
      color: #fff;
    }

    &::before {
      content: "📤";
      display: inline-block;
      position: relative;
      right: .1em;
      height: 1em;
      width: 1em;
    }
  }
}
